import { Component, Input } from '@angular/core';

@Component({
  selector: 'app-post-share-to',
  template: ``,
  styles: [
  ],
})
export class PostShareToComponent {

  @Input()
  flow: string = 'row';

}
